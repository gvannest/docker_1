# service est une commande qui permet de gerer les services dans un environnement
# orchestrer (swarm).
# create permet de créer un nouveau service
# --name définit le nom du service
# --replicat indique le nombre de fois que l'on souhaite que le containeur soit
# répéter.
# --env définit des variable d'environnement (se réferer au spécification des conteneur dans dockerhub)
# 42school/engineering-bay le nom du containeur
docker service create \
        --name engineering-bay \
        --replicas 2 \
        --network overmind \
        --env OC_USERNAME='rabbit' \
        --env OC_PASSWD='rabbit_password' \
        42school/engineering-bay
