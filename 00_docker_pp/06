# option -d pour mode détacher (arrière plan)
# --name pour donner un nom à la machine
# -p pour le port fowarding <port-hôte>|<port-invité>
# nginx le nom de l'image
docker run -d --name overlord -p 5000:80 nginx
